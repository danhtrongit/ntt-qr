{
  "name": "qr-promotion-system",
  "version": "1.0.0",
  "description": "QR Code Promotion System for sellers to generate and validate promotional codes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "pm2:start": "npx pm2 start ecosystem.config.js",
    "pm2:stop": "npx pm2 stop qr-promotion-system",
    "pm2:restart": "npx pm2 restart qr-promotion-system",
    "pm2:reload": "npx pm2 reload qr-promotion-system",
    "pm2:delete": "npx pm2 delete qr-promotion-system",
    "pm2:logs": "npx pm2 logs qr-promotion-system",
    "pm2:monit": "npx pm2 monit",
    "pm2:status": "npx pm2 status",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "qr-code",
    "promotion",
    "express",
    "sqlite"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "qrcode": "^1.5.4",
    "sqlite3": "^5.1.7",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "pm2": "^6.0.8"
  }
}
